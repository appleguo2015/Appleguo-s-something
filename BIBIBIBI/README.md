# BIBIBIBI

这里存放不参与程序运行、构建或发布的制作素材与历史文件。它们被保留下来，方便
以后重新编辑角色、背景或音素，但不应放回发布包。

- `blender/`: Blender 源文件、备份和导出测试文件。
- `original-images/`: 导出前的根目录图片副本；运行与图标使用 `assets/images/`。
- `scratch-projects/`: 用于提取音素的 Scratch 工程；运行使用 `assets/phonemes/` 中已提取的 WAV。
- `system-metadata/`: Finder `.DS_Store` 元数据。
